# ============================================================================
# Agent Core NG — Переменные окружения
# ============================================================================
# Скопируйте этот файл в .env и заполните нужные значения:
#   cp .env.example .env
#   nano .env
#
# Затем запустите систему:
#   make run          # локальный запуск
#   docker compose up # Docker-режим
# ============================================================================

# --- PostgreSQL (обязательно для agent-service) ---
DATABASE_URL=postgres://agentcore:agentcore@localhost:5432/agentcore?sslmode=disable
# Или отдельные параметры (если DATABASE_URL не задан):
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=agentcore
# DB_PASSWORD=agentcore
# DB_NAME=agentcore

# --- Порты сервисов ---
AGENT_SERVICE_PORT=8083
TOOLS_SERVICE_PORT=8082
MEMORY_SERVICE_PORT=8001
GATEWAY_PORT=8080

# --- URL сервисов (для связи между микросервисами) ---
AGENT_SERVICE_URL=http://localhost:8083
TOOLS_SERVICE_URL=http://localhost:8082
MEMORY_SERVICE_URL=http://localhost:8001
BROWSER_SERVICE_URL=http://localhost:8084
GATEWAY_URL=http://localhost:8080

# --- CORS (разрешённые домены для фронтенда) ---
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# --- Ollama (локальные LLM-модели) ---
OLLAMA_URL=http://localhost:11434

# --- LM Studio (альтернатива Ollama, локальные модели) ---
# LM_STUDIO_URL=http://localhost:1234/v1

# --- ChromaDB / RAG (memory-service) ---
CHROMA_DIR=./memory-service/data/chroma
TEMP_DIR=./memory-service/data/temp
EMBEDDING_MODEL=all-MiniLM-L6-v2
# CHROMA_URL=        # URL внешнего ChromaDB (если не встроенный)
# RAG_TOP_K=5        # Количество результатов RAG-поиска

# --- Web-UI ---
VITE_API_URL=http://localhost:8080

# ============================================================================
# Облачные LLM-провайдеры (опционально)
# ============================================================================

# --- OpenRouter (100+ моделей через один ключ) ---
# Получите на https://openrouter.ai/keys
# OPENROUTER_API_KEY=sk-or-...

# --- Routeway (70+ моделей, 200 бесплатных запросов/день) ---
# Получите на https://routeway.ai
# ROUTEWAY_API_KEY=...

# --- OpenAI ---
# OPENAI_API_KEY=sk-...

# --- Anthropic (Claude) ---
# ANTHROPIC_API_KEY=sk-ant-...

# --- YandexGPT ---
# YANDEXGPT_API_KEY=...
# YANDEXGPT_FOLDER_ID=...

# --- GigaChat (Сбер) ---
# GIGACHAT_CLIENT_ID=...
# GIGACHAT_CLIENT_SECRET=...
# GIGACHAT_SCOPE=GIGACHAT_API_PERS

# ============================================================================
# Облачные хранилища (опционально)
# ============================================================================

# --- Яндекс.Диск ---
# Получите OAuth-токен на https://oauth.yandex.ru/
# YANDEX_DISK_TOKEN=y0_...

# ============================================================================
# Аутентификация tools-service (RBAC)
# ============================================================================
# Формат: token1:role1,token2:role2,token3:role3
# Роли: viewer (чтение), operator (мутации), admin (полный доступ)
# Если не задан — legacy-режим без аутентификации (с предупреждением)
# TOOLS_AUTH_TOKENS=mytoken1:viewer,mytoken2:operator,mytoken3:admin
