# Agent Core NG — Подробный план разработки и текущий статус

---

## 1. Философия и цели проекта

### 1.1. Философия
Agent Core NG — это не просто чат-бот. Это полноценная система с единственным AI-агентом Admin, который живёт на компьютере пользователя, имеет полный доступ к его файловой системе, может выполнять системные команды и учиться на каждом взаимодействии. Система построена на принципах:

- **Автономность** — агент Admin выполняет задачи самостоятельно, без пошаговых подсказок от пользователя
- **Обучаемость** — каждая модель накапливает свою базу знаний и становится лучше со временем
- **Модульность** — каждый компонент является независимым микросервисом, который можно заменить или обновить отдельно
- **Безопасность** — все операции с файлами и командами проходят через контролируемые API
- **Расширяемость** — легко добавлять новых провайдеров LLM, облачные хранилища, инструменты

### 1.2. Цели проекта
1. Создать полностью переработанную версию Agent Core с нуля на микросервисной архитектуре
2. Обеспечить работу с локальными (Ollama) и облачными (OpenAI, Anthropic, YandexGPT, GigaChat, OpenRouter) LLM
3. Реализовать систему обучения — каждая модель учится на своих взаимодействиях
4. Интегрировать облачные хранилища (Яндекс.Диск) для работы с файлами
5. Единый агент Admin с полным доступом к ПК и адаптацией под размер модели
6. Обеспечить простую установку на Linux как обычной программы
7. Реализовать рабочие пространства (Workspaces) для изоляции проектов

---

## 2. Архитектура системы

### 2.1. Общая схема

```
┌─────────────────────────────────────────────────────────────────────┐
│                    Клиент (браузер, localhost:5173)                  │
│                         web-ui (React/Vite)                         │
└─────────────────────────────┬───────────────────────────────────────┘
                              │ HTTP
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│                   API Gateway (Go, :8080)                           │
│  Единая точка входа для всех запросов                               │
│  CORS: настраиваемый белый список доменов                           │
│  Маршрутизация по префиксам + прямые маршруты                      │
└───────┬──────────────────┬──────────────────┬───────────────────────┘
        │                  │                  │
        ▼                  ▼                  ▼
┌───────────────┐  ┌───────────────┐  ┌────────────────────────┐
│ memory-service│  │ tools-service │  │    agent-service        │
│ Python/FastAPI│  │     Go        │  │        Go               │
│ :8001         │  │ :8082         │  │ :8083                   │
│               │  │               │  │                         │
│ ChromaDB      │  │ Команды       │  │ Агент Admin             │
│ Обучение      │  │ Файлы         │  │ Адаптация под модель    │
│ RAG-поиск     │  │ Система       │  │ LLM провайдеры          │
│               │  │ Яндекс.Диск  │  │ PostgreSQL              │
└───────────────┘  └───────────────┘  └────────────────────────┘
```

### 2.2. Описание микросервисов

#### memory-service (Python/FastAPI, порт 8001)
**Назначение:** Долговременная память системы. Хранит факты, знания, индексированные файлы. Обеспечивает семантический поиск (RAG) и систему обучения моделей.

**Технологии:**
- Python 3.10+ / FastAPI — асинхронный веб-фреймворк
- ChromaDB — векторная база данных для хранения эмбеддингов
- sentence-transformers — модель `all-MiniLM-L6-v2` для создания эмбеддингов
- Pydantic — валидация данных

**Эндпоинты:**
| Метод | Путь | Описание |
|-------|------|----------|
| POST | `/facts` | Добавление факта в базу знаний |
| GET | `/search?q=...` | Семантический поиск по базе знаний |
| POST | `/files` | Индексация файла (разбиение на чанки, векторизация) |
| POST | `/learn` | Сохранение знания с привязкой к конкретной модели |
| GET | `/recall` | Извлечение знаний для конкретной модели по запросу |
| GET | `/learning-stats` | Статистика обучения модели (количество знаний, последнее обновление) |
| GET | `/health` | Проверка здоровья сервиса |

**Система обучения:**
- Каждая модель имеет свою изолированную коллекцию знаний в ChromaDB
- Формат коллекции: `learning_{model_name}` (например, `learning_llama3.1:8b`)
- При обучении сохраняются: вопрос пользователя, ответ модели, извлечённые факты, контекст
- При запросе `recall` возвращаются TOP-5 наиболее релевантных знаний

#### tools-service (Go, порт 8082)
**Назначение:** Выполнение системных команд, работа с файлами, мониторинг системы, интеграция с облачными хранилищами.

**Безопасность:**
- Белый список разрешённых команд (sudo, rm, reboot, kill и др. — агент = полный админ ПК)
- Валидация путей для файловых операций
- Таймауты на выполнение команд

**Эндпоинты:**
| Метод | Путь | Описание |
|-------|------|----------|
| POST | `/execute` | Выполнение системной команды |
| POST | `/read` | Чтение файла |
| POST | `/write` | Запись файла |
| POST | `/list` | Список файлов в директории |
| POST | `/delete` | Удаление файла |
| GET | `/sysinfo` | Полная информация о системе |
| GET | `/cpuinfo` | Информация о процессоре |
| GET | `/meminfo` | Информация об оперативной памяти |
| GET | `/cputemp` | Температура процессора |
| GET | `/sysload` | Загрузка системы |
| POST | `/findapp` | Поиск приложения |
| POST | `/launchapp` | Запуск приложения |
| POST | `/addautostart` | Добавление в автозагрузку |

**Яндекс.Диск (REST API):**
| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/ydisk/info` | Информация о диске (объём, занятое место, корзина) |
| GET | `/ydisk/list?path=/` | Содержимое папки с пагинацией |
| GET | `/ydisk/download?path=...` | Скачивание файла |
| POST | `/ydisk/upload` | Загрузка файла на диск |
| POST | `/ydisk/mkdir` | Создание папки |
| POST | `/ydisk/delete` | Удаление файла/папки (в корзину или безвозвратно) |
| POST | `/ydisk/move` | Перемещение/переименование файла |
| GET | `/ydisk/search?media_type=...` | Поиск файлов по типу медиа |

#### agent-service (Go, порт 8083)
**Назначение:** Ядро системы. Управление единственным агентом Admin, подключение к LLM-провайдерам, обучение, рабочие пространства.

**Агент:**
- **admin** — единственный агент, полный доступ к ПК. Сильные модели (7B+) получают базовые инструменты, слабые (≤3B) — составные навыки (LEGO-блоки)

**LLM-провайдеры (реестр):**
| Провайдер | Переменные окружения | Описание |
|-----------|---------------------|----------|
| Ollama | `OLLAMA_URL` | Локальные модели |
| OpenAI | `OPENAI_API_KEY` | GPT-4, GPT-4o |
| Anthropic | `ANTHROPIC_API_KEY` | Claude Sonnet, Opus |
| YandexGPT | `YANDEX_API_KEY`, `YANDEX_FOLDER_ID` | Российский провайдер |
| GigaChat | `GIGACHAT_CLIENT_SECRET`, `GIGACHAT_CLIENT_ID` | Российский провайдер (Сбер) |
| OpenRouter | `OPENROUTER_API_KEY` | 100+ моделей через один API-ключ |

**Автоматическая классификация моделей:**
- При первом появлении модели система определяет её возможности
- Проверяется поддержка tool calling (function calling)
- На основе характеристик определяется набор инструментов для агента Admin
- Результат сохраняется в PostgreSQL для повторного использования

#### api-gateway (Go, порт 8080)
**Назначение:** Единая точка входа для клиента. Маршрутизация запросов, CORS-защита.

**Маршрутизация:**
| Префикс | Направление | Описание |
|---------|-------------|----------|
| `/memory/` | memory-service:8001 | RAG, факты, обучение |
| `/tools/` | tools-service:8082 | Команды, файлы |
| `/agents/` | agent-service:8083 | Агенты, чат |
| `/ydisk/` | tools-service:8082 | Яндекс.Диск |
| `/chat` | agent-service:8083 | Отправка сообщения |
| `/models` | agent-service:8083 | Список моделей |
| `/providers` | agent-service:8083 | Облачные провайдеры |
| `/workspaces` | agent-service:8083 | Рабочие пространства |
| `/learning-stats` | agent-service:8083 | Статистика обучения |

### 2.3. Системные зависимости
- **PostgreSQL 14+** — хранение истории чатов, конфигурации агентов, моделей
- **Docker** — опционально, для запуска memory-service в контейнере
- **Go 1.22+** — компиляция Go-сервисов
- **Python 3.10+** — memory-service
- **Node.js 20+** — сборка веб-интерфейса
- **Ollama** — локальные LLM-модели (опционально)

### 2.4. Запуск как системные сервисы
При установке через `install.sh` создаются systemd-юниты:
- `agent-memory.service` — memory-service
- `agent-tools.service` — tools-service
- `agent-agent.service` — agent-service
- `agent-gateway.service` — api-gateway
- `agent-web.service` — web-ui (production build)

---

## 3. Реализованный функционал

### 3.1. Инфраструктура
- Микросервисная архитектура: 4 сервиса + web-ui
- API Gateway с маршрутизацией и CORS
- CI/CD через GitHub Actions (4 проверки: agent-service, tools-service, api-gateway, web-ui)
- PostgreSQL для хранения данных
- ChromaDB для векторной памяти

### 3.2. Агент Admin и LLM
- Единый агент Admin с адаптацией под размер модели
- 6 LLM-провайдеров: Ollama, OpenAI, Anthropic, YandexGPT, GigaChat, OpenRouter
- Автоматическое определение поддержки инструментов для моделей
- Адаптация: сильные модели (7B+) получают базовые инструменты, слабые (≤3B) — составные навыки (LEGO-блоки)

### 3.3. Память и обучение
- Векторная память (RAG) с ChromaDB
- Семантический поиск по базе знаний
- Система обучения: каждая модель накапливает свои знания
- Индексация файлов с разбиением на чанки
- Статистика обучения через API

### 3.4. Облачные хранилища
- Яндекс.Диск REST API: полный клиент (просмотр, загрузка, скачивание, удаление, перемещение, копирование, поиск)
- HTTP-обработчики в tools-service
- Маршруты в API Gateway

### 3.5. Веб-интерфейс
- Чат с агентом Admin
- Сворачиваемая панель выбора моделей
- Аватар агента с пульсацией
- Прикрепление файлов (📎)
- Голосовой ввод (🎤) через Web Speech API
- RAG-поиск
- Блоки кода с кнопкой копирования
- Рабочие пространства (Workspaces)

### 3.6. Инструменты Админа
- Полный доступ к файловой системе
- Выполнение системных команд без ограничений
- Настройка модели и промпта через UI
- 10 тестовых задач возрастающей сложности для проверки

### 3.7. Браузерные инструменты (browser-service)
- **Отдельный MCP-микросервис** на порту 8084
- **Навигация:** открытие URL (headless/visible), получение DOM/текста/заголовка, скриншоты PNG, PDF, выполнение JS
- **Ввод:** клавиатура (xdotool), мышь (клик, скролл, перемещение, драг), управление окнами (wmctrl), вкладками, буфер обмена (xclip)
- **Поиск:** DuckDuckGo и SearXNG (бесплатно, без API-ключей, локализация для РФ)
- **Краулер:** HTTP-запросы с маскировкой под Googlebot, YandexBot, Bingbot, MailRu
- **Доступность:** проверка URL (DNS→TCP→TLS→HTTP), санкции, обнаружение CAPTCHA
- **40+ HTTP-эндпоинтов** в cmd/server/main.go
- **20+ определений инструментов** для LLM в agent-service/internal/tools/list.go

### 3.8. Система навыков (Skills)
- **YAML-файлы** в директории `skills/` — каждый навык описывает: имя, параметры, эндпоинт, шаблон, теги, доступные агенты
- **Загрузчик** в `agent-service/internal/skills/loader.go` — парсинг YAML, поиск по тегам, фильтрация по роли, выполнение с подстановкой параметров
- **5 встроенных навыков:** web_search, screenshot, crawler_fetch, check_access, page_to_pdf
- **Расширяемость:** добавление новых навыков — просто создать .yaml файл в skills/

### 3.9. Динамическая конфигурация (без хардкода)
- **Принцип:** ни один URL или пользовательский путь не захардкожен в коде
- **Паттерн:** `getEnv("VAR_NAME", "default_value")` — все сервисные URL настраиваются через переменные окружения
- **Переменные окружения:**
  | Переменная | Умолчание | Описание |
  |------------|-----------|----------|
  | `TOOLS_SERVICE_URL` | `http://localhost:8082` | Адрес tools-service |
  | `BROWSER_SERVICE_URL` | `http://localhost:8084` | Адрес browser-service |
  | `MEMORY_SERVICE_URL` | `http://localhost:8001` | Адрес memory-service |
  | `AGENT_SERVICE_URL` | `http://localhost:8083` | Адрес agent-service |
  | `GATEWAY_URL` | `http://localhost:8080` | Адрес API Gateway |
  | `OLLAMA_URL` / `OLLAMA_HOST` | `http://localhost:11434` | Адрес Ollama |
  | `LM_STUDIO_URL` | `http://localhost:1234/v1` | Адрес LM Studio |
  | `VITE_API_URL` | `http://localhost:8080` | URL бэкенда для фронтенда |
- **Резолв путей:** `os.UserHomeDir()` для `~`, пустых путей, `~/subdir` — не привязан к конкретному пользователю
- **Системные пути** (`/var/lib/flatpak`, `/usr/local/share`) — допустимы, это стандартные Linux-пути

### 3.10. Подробное логирование
- **Цель:** Админ-агент может читать логи и понимать результаты выполнения инструментов
- **Префиксы:** `[TOOLS]` для HTTP-обработчиков, `[FILES]` для файловых операций, `[SYSTEM]` для системных функций
- **Что логируется:** входные параметры, результаты, ошибки, время выполнения
- **Пример:** `[TOOLS] list() called with path="/home/user/docs" → found 15 entries`

### 3.11. Тестирование
- **ТЕСТ 0 (Базовый чат):** агент отвечает через Routeway/OpenRouter — PASS
- **ТЕСТ 1 (Tool calling Админа):** 5/5 критериев — list(), sysinfo(), execute(), read(), write() — PASS
- **Оставшиеся тесты:** ТЕСТ 2 (cron), ТЕСТ 3 (файлы), ТЕСТ 4 (оркестрация), ТЕСТ 5 (память), ТЕСТ 6 (обучение)

---

## 4. Что ещё нужно реализовать

### 4.1. Ближайшие задачи (приоритет: высокий)

1. **Установщик для Linux (install.sh)**
   - Автоматическая установка всех зависимостей
   - Сборка Go-сервисов и web-ui
   - Создание systemd-юнитов
   - Настройка PostgreSQL
   - Запуск всех сервисов

2. **Тестирование в реальном браузере**
   - Запуск всех бэкенд-сервисов
   - Тестирование чата с агентами
   - Тестирование облачных провайдеров (OpenRouter, YandexGPT, GigaChat)
   - Тестирование Яндекс.Диска
   - Тестирование системы обучения

3. **Hardening — 6 фаз укрепления системы**
   - Фаза 1: Надёжность — структурированное JSON-логирование, correlation-ID, graceful shutdown
   - Фаза 2: Безопасность — Docker multi-stage, non-root, whitelist, input sanitization
   - Фаза 3: Высокая нагрузка — k6 тесты, per-user rate limiting
   - Фаза 4: Контракты — API versioning, OpenAPI спецификации
   - Фаза 5: Продакшн — Kubernetes, Prometheus метрики, tracing
   - Фаза 6: Аудит безопасности — RCE, SSRF, command injection, security scan

### 4.2. Рекомендации по улучшению

4. **Unit-тесты для основных модулей**
   - agent-service: repository, models, llm, usecase
   - tools-service: executor, sandbox
   - api-gateway: middleware (rate limiting, circuit breaker, tracing)
   - Проверка покрытия тестами (coverage)

5. **Интеграционные тесты memory-service**
   - FastAPI TestClient для всех эндпоинтов
   - Тесты RAG-поиска, обучения, индексации файлов
   - Проверка граничных случаев (большие файлы, пустые запросы)

6. **OpenAPI спецификации для всех сервисов**
   - Полные OpenAPI 3.0 спецификации для agent-service, tools-service, memory-service, api-gateway
   - Валидация запросов/ответов по спецификациям
   - Версионирование API (v1/ префикс)

7. **Tracing и мониторинг**
   - OpenTelemetry tracing для всех межсервисных вызовов
   - Prometheus метрики + Grafana дашборды
   - Структурированное логирование (JSON) с correlation-ID

8. **Security scan и аудит безопасности**
   - Статический анализ кода (SAST) в CI
   - Проверка зависимостей на уязвимости
   - Аудит tools-service: RCE, SSRF, command injection, container escape

### 4.3. Низкий приоритет (перспектива)

9. **Взаимодействие агента с браузером**
   - Агент открывает вкладки в Chrome, взаимодействует с AI-чатами
   - Chrome AI интеграция (Gemini Nano, Prompt API)

10. **Индексация облачных файлов в RAG**
    - Автоматическая индексация файлов с Яндекс.Диска, синхронизация

11. **Многопользовательский режим**
    - Аутентификация, авторизация, изоляция данных

12. **Упаковка в deb-пакет**
    - Бинарники в `/usr/bin`, конфиги в `/etc/agent-core`, данные в `/var/lib/agent-core`

---

## 5. Текущие ограничения и известные проблемы

- Однопользовательский режим (нет аутентификации)
- При загрузке аватара может потребоваться перезагрузка страницы (кэш браузера)
- Модели без поддержки инструментов помечаются ⚠️, но могут использоваться — tool calling будет недоступен
- WebSocket не реализован — используется HTTP polling для обновлений

---

## 6. Как продолжить работу

### Для разработчика
1. Клонируйте репозиторий, установите зависимости
2. Настройте переменные окружения (PostgreSQL, API-ключи)
3. Запустите сервисы (см. README.md)
4. Изучите структуру: каждый сервис независим

### Для ИИ-ассистента
1. Вся информация о проекте — в этом файле, README.md и ROADMAP.md
2. Код: `memory-service/`, `tools-service/`, `agent-service/`, `api-gateway/`, `web-ui/`
3. CI: `.github/workflows/ci.yml` — Go build/fmt/vet/тесты, Python тесты, TypeScript/Vite, security scan
4. Репозиторий: https://github.com/neo-2022/agent-RegArt
5. Все комментарии в коде — на русском языке
6. Единый агент Admin (не coder/novice)

---

## 7. История изменений

- **2026-02-18:** Создана базовая инфраструктура (memory-service, tools-service, agent-service, api-gateway), настроен gateway, залито на GitHub.
- **2026-02-19 (утро):** Аватары агентов, выбор промптов, оркестрация, автоопределение поддержки инструментов, улучшен интерфейс.
- **2026-02-19 (день):** Комплексный рефакторинг: безопасность, CORS, история в PostgreSQL, RAG в UI, обновление документации.
- **2026-02-19 (вечер):** Облачные LLM-провайдеры (OpenAI, Anthropic, YandexGPT, GigaChat, OpenRouter), Яндекс.Диск REST API, система обучения агентов, рабочие пространства, автоклассификация моделей, подробная документация.
- **2026-02-19 (ночь):** browser-service — отдельный MCP-микросервис браузерного взаимодействия (headless Chrome, xdotool, wmctrl, DuckDuckGo, маскировка под ботов, проверка доступности URL, CAPTCHA). Система навыков (YAML + загрузчик). 20+ определений инструментов для LLM.
- **2026-02-20:** Убран хардкод URL — все сервисные адреса через переменные окружения (getEnv). Подробное логирование с префиксами `[TOOLS]`, `[FILES]`, `[SYSTEM]` для Админ-агента. Динамический резолв путей (~, пустые, ~/subdir) через os.UserHomeDir(). LM Studio и Routeway провайдеры. Reasoning-модели (stripThinkingTags). Tool call chaining (до 5 раундов). Compound Skills. CI/CD: GitHub Actions (4 проверки). ТЕСТ 0 и ТЕСТ 1 — PASS.
- **2026-02-20 (вечер):** Единый агент Admin (удалены coder/novice). CI расширен: Go тесты, Python тесты, security scan. Документация, Makefile, ROADMAP, .env.example, Docker Compose.
- **2026-02-21:** Архитектурные улучшения: Clean Architecture (agent-service), Circuit Breaker, Rate Limiting, Sandbox (tools-service), Tracing middleware, RAG TTL, E2E тесты, OpenAPI спецификации.
- **2026-02-21 (вечер):** Безопасность и надёжность v0.2.1: DangerousCommands, path traversal защита, SSRF-защита, size limits, AppError, RAG token limiting, ErrorBoundary.
- **2026-02-22:** Полный перевод комментариев и документации на русский язык.
- **2026-02-22 (вечер):** Hardening — начало реализации 6 фаз укрепления системы. Фаза 1: структурированное JSON-логирование (log/slog), correlation-ID, graceful shutdown.
