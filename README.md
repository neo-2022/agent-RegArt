# Agent Core NG (Next Generation)

## Описание проекта

**Agent Core NG** — это полностью переработанная версия системы Agent Core, построенная на микросервисной архитектуре. Проект предназначен для локального запуска на ПК под управлением Linux и предоставляет мощного AI-ассистента с тремя ролями агентов (Админ, Кодер, Послушник), возможностью выполнения системных команд, работы с файлами, использования долговременной памяти (RAG), интеграции с облачными хранилищами и подключения облачных LLM-моделей.

Система поддерживает как локальные модели через Ollama (Llama, Qwen, Nemotron и др.), так и облачные провайдеры: OpenAI, Anthropic, YandexGPT, GigaChat и OpenRouter (доступ к 100+ моделям через единый API). Каждый агент может использовать свою модель, а система автоматически определяет, на какую роль подходит модель.

---

## Основные возможности

### AI-агенты с разделением ролей
- **Админ** — полный доступ к ПК: выполнение команд, управление файлами, настройка других агентов, подбор моделей
- **Кодер** — написание и отладка кода, работа с Git, компиляция, тестирование
- **Послушник (Novice)** — простые задачи, поиск информации, ответы на вопросы

### LLM-провайдеры (локальные + облачные)
- **Ollama** — локальные модели (Llama 3.1, Qwen 2.5, Nemotron и др.)
- **LM Studio** — локальные модели через OpenAI-совместимый API
- **OpenAI** — GPT-4, GPT-4o, GPT-3.5-turbo
- **Anthropic** — Claude Sonnet, Haiku, Opus
- **YandexGPT** — российский облачный провайдер от Яндекса
- **GigaChat** — российский облачный провайдер от Сбера
- **OpenRouter** — агрегатор 100+ моделей (OpenAI, Anthropic, Google, Meta, Mistral, DeepSeek и др.) через один API-ключ
- **Routeway** — облачный провайдер с поддержкой reasoning-моделей

### Облачное хранилище — Яндекс.Диск
- Просмотр файлов и папок на Яндекс.Диске
- Загрузка файлов на диск и скачивание с диска
- Создание и удаление папок
- Перемещение и копирование файлов
- Поиск файлов по типу медиа (документы, изображения, видео и др.)

### Система обучения агентов
- Каждая модель накапливает свою отдельную базу знаний
- После успешных взаимодействий извлекаются ключевые факты и паттерны
- Перед каждым запросом подгружаются релевантные знания из базы
- Знания хранятся в ChromaDB с привязкой к конкретной модели
- Статистика обучения доступна через API

### Долговременная память (RAG)
- Векторная база данных ChromaDB для хранения фактов
- Semantic search — поиск по смыслу, а не по ключевым словам
- Индексация файлов из локальной файловой системы
- Автоматическое разбиение больших файлов на чанки
- Подключение контекста из RAG к запросам агентов

### RAG модуль (новое)
См. [документацию](docs/rag-module.md):
- **Локальные эмбеддинги** - вычисление векторов на CPU без внешних сервисов
- **Chroma интеграция** - векторное хранилище для семантического поиска
- **Multi-hop поиск** - многоходовый поиск с расширением запроса
- **Гибридный поиск** - комбинация BM25 + семантики (в разработке)

### Embeddings модуль
См. [документацию](docs/embeddings-module.md):
- Локальная модель эмбеддингов на CPU
- Детерминированные вектора (SHA256 seed)
- Нормализация и косинусное сходство
- Планируется: SentenceTransformer, OpenAI embeddings

### Vector-Store модуль
См. [документацию](docs/vector-store-module.md):
- Интеграция с ChromaDB
- REST API для добавления и поиска документов
- Кэширование и индексирование

### Рабочие пространства (Workspaces)
- Отдельная история чатов для каждого пространства
- Отдельная конфигурация агентов (модели, промпты)
- Привязка к рабочей директории на ПК
- Переключение между пространствами в сайдбаре UI

### Браузерные инструменты Админа (browser-service)
- **Отдельный MCP-микросервис** на порту 8084 — подключается при необходимости
- Навигация: открытие URL (headless/visible), получение DOM, текста, заголовка
- Скриншоты и PDF: захват страниц в PNG/PDF через headless Chrome
- Ввод: клавиатура, мышь, скроллинг через xdotool
- Управление окнами: фокус, перемещение, размер, свернуть/развернуть через wmctrl
- Управление вкладками: новая, закрыть, переключение, переоткрытие
- Буфер обмена: копирование, вставка через xclip/xsel
- HTTP-запросы: GET/POST/PUT/DELETE/PATCH/HEAD/OPTIONS
- Поиск в интернете: DuckDuckGo, SearXNG (бесплатно, без API-ключей, работает в РФ)
- Маскировка под ботов: Googlebot, YandexBot, Bingbot для обхода блокировок
- Проверка доступности URL: санкции, блокировки, обнаружение CAPTCHA
- Выполнение JavaScript на страницах

### Система навыков (Skills)
- YAML-описания навыков в директории `skills/`
- Загрузчик навыков с фильтрацией по роли агента
- Шаблоны запросов с подстановкой параметров
- 5 встроенных навыков: поиск, скриншот, краулер, доступность, PDF

### Веб-интерфейс
- Чат с агентами, переключение по именам/аватарам
- Сворачиваемая панель выбора моделей (локальные + облачные)
- Прикрепление файлов (📎) и голосовой ввод (🎤) через Web Speech API
- RAG-поиск с визуализацией результатов
- Блоки кода с подсветкой синтаксиса и кнопкой копирования
- Аватары агентов с пульсацией активного

---

## Архитектура

```
┌─────────────────────────────────────────────────────────────────────┐
│                    Клиент (браузер, localhost:5173)                  │
│                         web-ui (React/Vite)                         │
└─────────────────────────────┬───────────────────────────────────────┘
                              │ HTTP
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│                   API Gateway (Go, :8080)                           │
│  Маршрутизация:                                                     │
│    /memory/*  → memory-service    /agents/* → agent-service         │
│    /tools/*   → tools-service     /ydisk/*  → tools-service         │
│    /chat, /models, /providers     → agent-service                   │
│    /workspaces, /learning-stats   → agent-service                   │
│  CORS: настраиваемый белый список доменов                           │
└───────┬──────────────────┬──────────────────┬───────────────────────┘
        │                  │                  │
        ▼                  ▼                  ▼
┌───────────────┐  ┌───────────────┐  ┌────────────────────────┐
│ memory-service│  │ tools-service │  │    agent-service        │
│ Python/FastAPI│  │     Go        │  │        Go               │
│ :8001         │  │ :8082         │  │ :8083                   │
│               │  │               │  │                         │
│ ChromaDB      │  │ Команды       │  │ Агенты (admin/coder/    │
│ Sentence-     │  │ Файлы         │  │   novice)               │
│  transformers │  │ Система       │  │ LLM провайдеры          │
│ RAG-поиск     │  │ Яндекс.Диск  │  │ PostgreSQL              │
│ Обучение      │  │ Приложения    │  │ Обучение моделей        │
└───────────────┘  └───────────────┘  └────────────────────────┘
                                              │
                                     ┌────────┴────────┐
                                     ▼                  ▼
                              ┌──────────────┐  ┌──────────────┐
                              │browser-service│  │   skills/    │
                              │    Go :8084   │  │  YAML-файлы  │
                              │               │  │  навыков     │
                              │ Chrome headless│ │              │
                              │ xdotool/wmctrl│  │ Загрузчик    │
                              │ DuckDuckGo    │  │ в agent-svc  │
                              │ Маскировка    │  │              │
                              └──────────────┘  └──────────────┘
                                              │
                              ┌────────────────┼────────────────┐
                              ▼                ▼                ▼
                        ┌──────────┐    ┌──────────┐    ┌──────────┐
                        │  Ollama  │    │ OpenAI/  │    │ YandexGPT│
                        │ (локал.) │    │ Anthropic│    │ GigaChat │
                        │          │    │ OpenRouter│   │          │
                        └──────────┘    └──────────┘    └──────────┘
```

### Описание микросервисов

#### memory-service (Python/FastAPI, порт 8001)
Микросервис для работы с векторной памятью и обучением агентов. Использует ChromaDB для хранения векторных представлений и sentence-transformers (модель `all-MiniLM-L6-v2`) для создания эмбеддингов.

**Эндпоинты:**
- `POST /facts` — добавление факта в базу знаний
- `GET /search?q=...` — семантический поиск по базе знаний
- `POST /files` — индексация файла (с разбиением на чанки)
- `POST /learn` — сохранение знания с привязкой к модели
- `GET /recall?model=...&query=...` — извлечение знаний конкретной модели
- `GET /learning-stats?model=...` — статистика обучения модели
- `GET /health` — проверка здоровья сервиса

#### tools-service (Go, порт 8082)
Микросервис для выполнения системных команд, работы с файлами, информации о системе и интеграции с Яндекс.Диском.

**Эндпоинты:**
- `POST /execute` — выполнение системной команды
- `POST /read` — чтение файла
- `POST /write` — запись файла
- `POST /list` — список файлов в директории
- `POST /delete` — удаление файла
- `GET /sysinfo` — информация о системе
- `GET /cpuinfo`, `/meminfo`, `/cputemp`, `/sysload` — мониторинг системы
- `POST /findapp`, `/launchapp`, `/addautostart` — управление приложениями

**Яндекс.Диск (REST API):**
- `GET /ydisk/info` — информация о диске (объём, занятое место)
- `GET /ydisk/list?path=/` — содержимое папки
- `GET /ydisk/download?path=...` — скачивание файла
- `POST /ydisk/upload` — загрузка файла на диск
- `POST /ydisk/mkdir` — создание папки
- `POST /ydisk/delete` — удаление файла/папки
- `POST /ydisk/move` — перемещение/переименование
- `GET /ydisk/search?media_type=...` — поиск файлов по типу

#### agent-service (Go, порт 8083)
Ядро системы — управление агентами, подключение к LLM, обучение, рабочие пространства.

**Эндпоинты:**
- `GET /agents` — список агентов с текущими моделями
- `POST /agents/chat` — отправка сообщения агенту
- `GET /models` — список доступных моделей (локальные + облачные)
- `POST /update-model` — обновление модели агента
- `GET /providers` — список облачных провайдеров
- `POST /providers` — регистрация нового провайдера
- `GET /cloud-models` — модели облачных провайдеров
- `GET /workspaces` — список рабочих пространств
- `POST /workspaces` — создание/обновление пространства
- `GET /learning-stats` — статистика обучения
- `POST /avatar` — загрузка аватара агента
- `GET /prompts` — список файлов промптов

#### api-gateway (Go, порт 8080)
Единая точка входа для всех запросов. Маршрутизирует запросы на соответствующие микросервисы, обеспечивает CORS-защиту.

#### web-ui (React/TypeScript/Vite, порт 5173)
Веб-интерфейс для взаимодействия с системой. SPA на React с Vite для разработки.

---

## Установка

### Системные требования

- **ОС:** Ubuntu 22.04+ (или другой Linux с systemd)
- **CPU:** 4+ ядер (рекомендуется AMD Ryzen 7+ / Intel Core i7+)
- **RAM:** 16 GB минимум (32+ GB рекомендуется для больших моделей)
- **GPU:** NVIDIA с 8+ GB VRAM для локальных моделей (опционально)
- **Диск:** 50+ GB свободного места (SSD рекомендуется)

### Предварительные требования

- Git
- Docker и Docker Compose
- Go 1.22+
- Python 3.10+ и pip
- PostgreSQL 14+
- Node.js 20+ и npm
- Ollama (для локальных LLM-моделей)

### Быстрая установка (install.sh)

```bash
git clone https://github.com/neo-2022/openclaw-memory.git AgentCore-NG
cd AgentCore-NG
chmod +x install.sh
sudo ./install.sh
```

Установщик автоматически:
- Установит все зависимости (Go, Node.js, Python, PostgreSQL, Docker)
- Соберёт все Go-сервисы
- Установит Python-зависимости для memory-service
- Соберёт web-ui
- Создаст systemd-сервисы для автозапуска
- Настроит PostgreSQL

### Ручная установка

#### 1. Клонирование репозитория

```bash
git clone https://github.com/neo-2022/openclaw-memory.git AgentCore-NG
cd AgentCore-NG
```

#### 2. Переменные окружения

Создайте файл `.env` в корне проекта или экспортируйте переменные:

```bash
# === PostgreSQL (для agent-service) ===
export DATABASE_URL="postgres://agent_user:password@localhost:5432/agent_db?sslmode=disable"

# === API Gateway (CORS) ===
export CORS_ALLOWED_ORIGINS="http://localhost:3000,http://localhost:5173"

# === Сервисные URL (все настраиваемые, значения по умолчанию) ===
export TOOLS_SERVICE_URL="http://localhost:8082"
export BROWSER_SERVICE_URL="http://localhost:8084"
export MEMORY_SERVICE_URL="http://localhost:8001"
export AGENT_SERVICE_URL="http://localhost:8083"
export GATEWAY_URL="http://localhost:8080"

# === Ollama (локальные модели) ===
export OLLAMA_URL="http://localhost:11434"   # или OLLAMA_HOST

# === LM Studio (локальные модели) ===
export LM_STUDIO_URL="http://localhost:1234/v1"

# === Облачные LLM-провайдеры (опционально) ===
export OPENAI_API_KEY="sk-..."              # OpenAI
export ANTHROPIC_API_KEY="sk-ant-..."       # Anthropic
export YANDEX_API_KEY="..."                 # YandexGPT
export YANDEX_FOLDER_ID="..."              # YandexGPT
export GIGACHAT_CLIENT_SECRET="..."         # GigaChat (Сбер)
export GIGACHAT_CLIENT_ID="..."             # GigaChat (Сбер)
export OPENROUTER_API_KEY="sk-or-v1-..."    # OpenRouter (100+ моделей)
export ROUTEWAY_API_KEY="..."               # Routeway

# === Облачное хранилище ===
export YANDEX_DISK_TOKEN="y0_..."           # Яндекс.Диск

# === Web-UI ===
export VITE_API_URL="http://localhost:8080"  # URL бэкенда для фронтенда
```

#### 3. Настройка PostgreSQL

```bash
sudo -u postgres psql -c "CREATE USER agent_user WITH PASSWORD 'your_password';"
sudo -u postgres psql -c "CREATE DATABASE agent_db OWNER agent_user;"
```

#### 4. Установка Ollama

```bash
curl -fsSL https://ollama.com/install.sh | sh
ollama pull llama3.1:8b
ollama pull qwen2.5-coder:7b
```

#### 5. Сборка и запуск

```bash
# memory-service
cd memory-service && pip install -r requirements.txt
python -m uvicorn app.main:app --host 0.0.0.0 --port 8001 &

# tools-service
cd ../tools-service && go build -o tools-service ./cmd/server/ && ./tools-service &

# agent-service
cd ../agent-service && go build -o agent-service ./cmd/server/ && ./agent-service &

# api-gateway
cd ../api-gateway && go build -o api-gateway ./cmd/ && ./api-gateway &

# web-ui
cd ../web-ui && npm install && npm run dev &
```

#### 6. Открытие в браузере

Перейдите на http://localhost:5173

---

## Конфигурация облачных провайдеров

### OpenRouter (рекомендуется — один ключ для 100+ моделей)

1. Зарегистрируйтесь на https://openrouter.ai
2. Создайте API-ключ: https://openrouter.ai/settings/keys
3. `export OPENROUTER_API_KEY="sk-or-v1-..."`

Доступные модели: GPT-4o, Claude 3.5 Sonnet, Gemini Pro, Llama 3.1, DeepSeek-V3 и ещё 100+.

### YandexGPT

1. Получите API-ключ: https://console.yandex.cloud
2. `export YANDEX_API_KEY="..." YANDEX_FOLDER_ID="..."`

### GigaChat (Сбер)

1. Зарегистрируйтесь: https://developers.sber.ru/portal/products/gigachat
2. `export GIGACHAT_CLIENT_SECRET="..." GIGACHAT_CLIENT_ID="..."`

### Яндекс.Диск

1. Получите OAuth-токен: https://oauth.yandex.ru
2. `export YANDEX_DISK_TOKEN="y0_..."`

---

## Структура проекта

```
AgentCore-NG/
├── agent-service/           # Сервис агентов (Go)
│   ├── cmd/server/          # Точка входа
│   └── internal/
│       ├── llm/             # LLM-провайдеры (ollama, openai, anthropic, yandex, gigachat, openrouter)
│       ├── models/          # Модели данных
│       ├── repository/      # Работа с PostgreSQL
│       ├── db/              # Подключение к БД
│       └── tools/           # Определения инструментов
├── tools-service/           # Сервис инструментов (Go)
│   ├── cmd/server/          # Точка входа + HTTP-обработчики (включая Яндекс.Диск)
│   └── internal/executor/   # Выполнение команд, файлы, система, облачное хранилище
├── memory-service/          # Сервис памяти (Python/FastAPI)
│   └── app/                 # FastAPI-приложение, ChromaDB, обучение
├── api-gateway/             # API Gateway (Go)
│   └── cmd/main.go          # Маршрутизация + CORS
├── browser-service/         # MCP-микросервис браузерного взаимодействия (Go)
│   ├── cmd/server/          # HTTP-сервер (порт 8084)
│   └── internal/
│       ├── browser/         # Навигация, скриншоты, PDF, DOM, headless Chrome
│       ├── input/           # Клавиатура, мышь, окна, вкладки, буфер обмена
│       ├── search/          # Поиск: DuckDuckGo, SearXNG (бесплатно, РФ)
│       ├── crawler/         # HTTP-запросы с маскировкой (Googlebot/YandexBot/Bingbot)
│       └── access/          # Проверка доступности URL, санкции, CAPTCHA
├── skills/                  # YAML-описания навыков агентов
│   ├── web_search.yaml      # Поиск в интернете
│   ├── screenshot.yaml      # Скриншот страницы
│   ├── crawler_fetch.yaml   # Загрузка с маскировкой
│   ├── check_access.yaml    # Проверка доступности
│   └── page_to_pdf.yaml     # Конвертация в PDF
├── web-ui/                  # Веб-интерфейс (React/Vite)
│   └── src/                 # Компоненты, стили
├── .github/workflows/       # CI/CD (GitHub Actions)
├── tasks/                   # Тестовые задачи для агентов
├── install.sh               # Установщик для Linux
├── README.md                # Этот файл
└── PLAN.md                  # Подробный план разработки
```

---

## CI/CD

GitHub Actions автоматически проверяет при каждом push и PR:
1. **agent-service** — `go build`, `gofmt`, `go vet`
2. **tools-service** — `go build`, `gofmt`, `go vet`
3. **api-gateway** — `go build`, `gofmt`, `go vet`
4. **web-ui** — `tsc --noEmit`, `vite build`

---

## Принципы кода

- **Хардкод запрещён:** все URL и пользовательские пути настраиваются через переменные окружения с разумными дефолтами
- **Подробное логирование:** каждый обработчик логирует вход/выход/ошибки с префиксами `[TOOLS]`, `[FILES]`, `[SYSTEM]`
- **Динамический резолв путей:** `~`, пустые пути, `~/subdir` автоматически разрешаются через `os.UserHomeDir()`

---

## Лицензия

MIT License

## Авторы

- **Neo** — архитектор и владелец проекта
